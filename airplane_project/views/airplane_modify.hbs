<h1>{{ title }}</h1>

<table>
    <tr>
        <td>Airplane_Id</td>
        <td>TOTAL_NUMBER_OF_SEATS</td>
        <td>Airplane_Type</td>
    </tr>
    <!-- dept_res 데이터가 있을 때 마다 반복-->
    {{#each airplane}}
    <form method="post"> <!--post에서처리-->
        <tr>
            <!-- input tag를 사용하여 입력 가능한 형태로 변경함(다른 방법도 검색하면 있음) -->
            <td><input type="text" name="airplane_id" value={{Airplane_Id}} readonly></td>
            <td><input type="text" name="total_number_of_seats" value="{{TOTAL_NUMBER_OF_SEATS}}" placeholder={{TOTAL_NUMBER_OF_SEATS}}></td>
            <td><input type="text" name="airplane_type" value="{{Airplane_Type}}" placeholder={{Airplane_Type}}></td>
            <!-- formation 속성을 활용하여 입력한 값을 넘겨줌) -->
            <td><input type="submit" value="수정" formaction="/admin/airplane/modify"></td>
        </tr>
    </form>   
    {{/each}}
</table>